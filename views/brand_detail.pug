extends layout

block content
  div.container
    h1 #{title} items

    a(href=brand.url+'/update')
      h4 Update Brand Name
      
    a(href=brand.url+'/update_image')
      h4 Update Brand Image
    
    div.contianer
      each val, index in item_list
        a(href=val.url) 
          div.list-item-container
            h3 #{val.name}
            h3 #{val.price}

      button(onclick='window.history.back()') Back